# Claude Code Entry Point

Follow `AGENTS.md`. Load specialist skills from `skills/` or installed `.claude/skills/`. Use the manager-agent sequence and maintain the canonical project specification before implementation.
