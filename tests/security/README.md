# Security Validation Contract

Automate secret scanning, dependency review, static analysis, and security headers. Add application-specific authorization and tenancy tests. Dynamic testing is restricted to authorized non-production environments by default.
